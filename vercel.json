{
  "buildCommand": "cd ../.. && pnpm --filter web build",
  "installCommand": "corepack enable && corepack prepare pnpm@9.0.0 --activate && cd ../.. && pnpm install --no-frozen-lockfile",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
